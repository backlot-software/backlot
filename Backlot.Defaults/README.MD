Backlot.Defaults contains scenarios and services which are optional for your installation.
But which, in someway, are used by 95% of all implementations.
The package is also used by certain other packages to make use of these defaults.