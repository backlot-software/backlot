---
phase: 04-role-editing
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - Pages/Roles/Edit.cshtml
  - Pages/Roles/Edit.cshtml.cs
  - Pages/Roles/Detail.cshtml
  - Pages/Roles/Detail.cshtml.cs
autonomous: true
requirements: [EDIT-02, EDIT-03]
must_haves:
  truths:
    - "Submitting the edit form with invalid data calls role/isvalid first, re-renders the form (422) with a summary block listing the validation errors, and does NOT call persist"
    - "Submitting valid data calls persist/persist and on success redirects (303) to /roles/{uid}, where a green 'Role saved.' banner appears"
    - "The success banner is driven by a redirect query flag (?saved=1) read by the detail page — no TempData is used anywhere"
    - "The persist payload is built from the schema field list (not posted keys), skipping Calculated/read-only fields, so arbitrary fields cannot be injected"
    - "A non-validation persist failure (HTTP 500 / network) re-renders the form with entered values preserved and an error message"
  artifacts:
    - path: "Pages/Roles/Edit.cshtml.cs"
      provides: "Hardened OnPostAsync: schema-driven BuildPayload, isvalid->422 summary, persist->303, persist-failure handling"
      contains: "BuildPayload"
    - path: "Pages/Roles/Edit.cshtml"
      provides: "alert-danger validation summary block rendering ValidationErrors"
      contains: "Please fix the following before saving"
    - path: "Pages/Roles/Detail.cshtml.cs"
      provides: "Saved query-flag bind property for the success banner"
      contains: "public bool Saved"
    - path: "Pages/Roles/Detail.cshtml"
      provides: "alert-success dismissible 'Role saved.' banner gated on ?saved=1"
      contains: "Role saved."
  key_links:
    - from: "Pages/Roles/Edit.cshtml.cs"
      to: "Pages/Roles/Detail.cshtml.cs"
      via: "TurboRedirect to /roles/{uid}?saved=1 sets the flag the detail page reads to show the banner (no TempData)"
      pattern: "saved=1"
    - from: "Pages/Roles/Edit.cshtml.cs"
      to: "Services/BacklotApiClient.cs"
      via: "OnPostAsync calls ValidateRoleAsync then PersistRoleAsync with the schema-built payload"
      pattern: "ValidateRoleAsync|PersistRoleAsync"
    - from: "Pages/Roles/Edit.cshtml"
      to: "Pages/Roles/Edit.cshtml.cs"
      via: "view renders Model.ValidationErrors (each ErrorMessage) in the summary block"
      pattern: "ValidationErrors"
---

<objective>
Harden the edit save path into its final form: full mass-assignment-safe payload building, the `role/isvalid` validation summary block (D-07), the `persist/persist` save with a 303 redirect, and the TempData-free "Role saved." success banner on the detail page (D-08). This delivers EDIT-02 and EDIT-03.

Builds directly on 04-01 (which created the page, base class, service methods, and proved the Turbo 303/422 contract). This plan replaces the minimal smoke-test POST handler with the production save orchestration and adds the banner read on Detail.

Purpose: Complete the only mutation flow in v1 — validate-then-persist with correct Turbo status codes and a clean, idiomatic success confirmation — honoring D-07 (summary block, not inline) and D-08 (NO TempData).
Output: Production `OnPostAsync` with `BuildPayload`, the validation summary block, and the `?saved=1` banner on Detail.
</objective>

<execution_context>
@/home/jeroen/Projects/Backlot/.claude/gsd-core/workflows/execute-plan.md
@/home/jeroen/Projects/Backlot/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-role-editing/04-CONTEXT.md
@.planning/phases/04-role-editing/04-RESEARCH.md
@.planning/phases/04-role-editing/04-PATTERNS.md
@.planning/phases/04-role-editing/04-UI-SPEC.md
@.planning/phases/04-role-editing/04-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (04-02) modifies/completes:
- `Pages/Roles/Edit.cshtml.cs` — hardened `OnPostAsync` with `BuildPayload(IReadOnlyList<FieldSchema>)` (schema-driven, type-coercing, Calculated-skipping), `ValidationErrors` populated on the 422 path, and persist-failure handling that preserves entered values.
- `Pages/Roles/Edit.cshtml` — `alert alert-danger` validation summary block ("Please fix the following before saving:") and the persist-failure error message.
- `Pages/Roles/Detail.cshtml.cs` — `[BindProperty(SupportsGet = true)] public bool Saved` query-flag bind.
- `Pages/Roles/Detail.cshtml` — `alert alert-success alert-dismissible` "Role saved." banner gated on `Model.Saved`.

Carried in from 04-01: `TurboRedirect`/`TurboInvalidPage` (TurboEditPageModel), the three `IBacklotApiClient` methods, `RoleSchema`/`ValidationOutcome` DTOs, the rendered edit form.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Production save orchestration — schema-driven BuildPayload, isvalid->422 summary, persist->303, persist-failure handling</name>
  <files>Pages/Roles/Edit.cshtml.cs, Pages/Roles/Edit.cshtml</files>
  <read_first>
    - .planning/phases/04-role-editing/04-RESEARCH.md (Pattern 1 303/422 lines 127-163; Pattern 2 BuildPayload lines 165-213; Q2 isvalid shape + defensive parse lines 255-269; Q4 success banner lines 280-287; Pitfall 1/2/3/5; Security mass-assignment + XSS lines 396-408)
    - .planning/phases/04-role-editing/04-PATTERNS.md (Edit.cshtml.cs OnPostAsync section lines 128-145; error summary block lines 178-187)
    - .planning/phases/04-role-editing/04-UI-SPEC.md (Copywriting Contract: validation summary, persist failure, lines 105-108; Interaction Contract lines 138-143)
    - Pages/Roles/Edit.cshtml.cs (the model from 04-01, including the minimal OnPostAsync and IsReadOnly/IsBool/IsNumeric helpers to reuse)
    - Pages/Roles/Edit.cshtml (the form from 04-01, to add the summary block)
    - Pages/Roles/Detail.cshtml.cs (ErrorMessage + catch pattern to mirror for persist failure)
    - .planning/phases/04-role-editing/04-01-SUMMARY.md (confirmed isvalid casing + any surprise characteristics from the live run)
  </read_first>
  <action>
    Replace the minimal `OnPostAsync` in `Pages/Roles/Edit.cshtml.cs` with the production orchestration. Add `private Dictionary<string, object?> BuildPayload(IReadOnlyList<FieldSchema> schema)`: start with `["Uid"] = Uid`; iterate the SCHEMA fields (authoritative list — never the posted keys, RESEARCH Security mass-assignment); skip any field where `IsReadOnly(f)` is true (Calculated); for each remaining field read `Fields.TryGetValue(f.Field, out var raw)` and coerce by Type via a `CoerceByType(string type, string? raw)` helper — `Boolean` -> `raw == "true"` (default `false` when the key is absent, handling the unchecked-checkbox case per RESEARCH Pitfall 3); numeric Types -> parse to the matching numeric (fall back to the raw string if parse fails so the API surfaces a validation error rather than the form crashing); everything else -> the raw string. Drive bool fields from the schema, not from posted keys, so an unchecked box still sends `false`.

    Rewrite the handler body: `SetUserContext()`; via `SafeApiCall` re-fetch `GetRoleSchemaAsync()` and `GetRoleDetailAsync(Uid)` (needed to re-match the schema row by `__Skills[0]` and to re-render the form on the 422 path with entered values); re-check `CanWrite` server-side (T-04-02 defense-in-depth) and if false short-circuit to the load-error/forbidden render; match the schema row (RESEARCH Pattern 3 with the base-type fallback); `var payload = BuildPayload(matchedFields)`; call `ValidateRoleAsync(payload)`; parse the outcome DEFENSIVELY (RESEARCH Q2): if outcome is null OR `!outcome.IsValid`, set `ValidationErrors = outcome?.Results ?? []`, and when Results is empty but invalid surface a single generic "Validation failed." item, then `return TurboInvalidPage()` (422) and do NOT call persist (D-06). If valid, call `PersistRoleAsync(payload)` inside a `try/catch (Exception ex) when (ex is HttpRequestException or TaskCanceledException)`: on success `return TurboRedirect($"/roles/{Uid}?saved=1")` (303); on failure set `ErrorMessage = "Save failed — the change was not stored. Your entries are preserved; try again."`, keep `Fields` (entered values already bound) so the form re-renders with them intact, and `return TurboInvalidPage()` (422 so Turbo swaps the body). Tolerate null `ErrorMessage` items in Results (RESEARCH Q2 defensive parse).

    Add the validation summary block to `Pages/Roles/Edit.cshtml` at the top of the form area (mirror the Detail `alert-danger` structure): `@if (Model.ValidationErrors.Any())` -> `<div class="alert alert-danger" role="alert">` with the heading "Please fix the following before saving:" and a `<ul class="mb-0">` listing each `@e.ErrorMessage` (D-07 summary block, NOT inline-per-field — this is the conscious EDIT-02 deviation). The persist-failure `ErrorMessage` renders via the existing `alert alert-danger` block already present from 04-01 (single message form). Do NOT use `Html.Raw` on any error text.
  </action>
  <acceptance_criteria>
    - Source: `Pages/Roles/Edit.cshtml.cs` contains a `BuildPayload` method and a `CoerceByType` helper. Verify: `grep -c -E 'BuildPayload|CoerceByType' Pages/Roles/Edit.cshtml.cs` returns >= 2.
    - Source: `BuildPayload` iterates schema fields and skips read-only — `IsReadOnly` is called inside the payload build. Verify: `grep -v '^#' Pages/Roles/Edit.cshtml.cs | grep -c 'IsReadOnly'` returns >= 1.
    - Source: the invalid path returns 422 via `TurboInvalidPage()` and the success path returns 303 via `TurboRedirect(`. Verify: `grep -c -E 'TurboInvalidPage|TurboRedirect' Pages/Roles/Edit.cshtml.cs` returns >= 2.
    - Source: persist is NOT called on the invalid path — `PersistRoleAsync` appears only after the `IsValid` check. (Manual read confirms ordering.)
    - Source: `Pages/Roles/Edit.cshtml` contains the summary heading. Verify: `grep -c 'Please fix the following before saving' Pages/Roles/Edit.cshtml` returns 1.
    - Source: no `Html.Raw` on error/field text. Verify: `grep -c 'Html.Raw' Pages/Roles/Edit.cshtml` returns 0.
    - Source: no TempData anywhere in the edit page. Verify: `grep -c 'TempData' Pages/Roles/Edit.cshtml.cs Pages/Roles/Edit.cshtml` returns 0.
    - Build: `dotnet build Backlot.Studio/Backlot.Studio.csproj` succeeds.
    - Behavior (against running API): POST invalid data -> HTTP 422, summary block lists the isvalid errors, persist NOT called (verify via API logs / no persisted change); POST valid data -> HTTP 303 to /roles/{uid}?saved=1; simulated persist failure -> form re-renders with entered values + "Save failed" message.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build Backlot.Studio/Backlot.Studio.csproj 2>&1 | grep -E 'Build succeeded|error'</automated>
    <human-check>Against the running API: submit invalid -> 422 + summary block of real isvalid errors, no persisted change; submit valid -> 303 to detail. Confirm no TempData and that unchecked bool fields persist as false.</human-check>
  </verify>
  <done>
    OnPostAsync validates via isvalid (422 + summary on failure, persist skipped), persists via persist/persist (303 on success), builds the payload mass-assignment-safe from the schema, handles persist failure by preserving entered values, and uses no TempData. EDIT-02 (summary-block per D-07) and the save half of EDIT-03 delivered.
  </done>
</task>

<task type="auto">
  <name>Task 2: TempData-free "Role saved." success banner on the detail page (?saved=1)</name>
  <files>Pages/Roles/Detail.cshtml.cs, Pages/Roles/Detail.cshtml</files>
  <read_first>
    - .planning/phases/04-role-editing/04-RESEARCH.md (Q4 success banner via ?saved=1 lines 280-287; open-redirect note in Security lines 407)
    - .planning/phases/04-role-editing/04-PATTERNS.md (Detail.cshtml(.cs) MODIFY section lines 248-268)
    - .planning/phases/04-role-editing/04-UI-SPEC.md (Color success semantics lines 91; Copywriting "Role saved." line 109; Component Inventory success banner line 134)
    - Pages/Roles/Detail.cshtml.cs (existing [BindProperty(SupportsGet=true)] Uid to mirror for Saved)
    - Pages/Roles/Detail.cshtml (existing alert-danger block to mirror as alert-success)
  </read_first>
  <action>
    Modify `Pages/Roles/Detail.cshtml.cs`: add `[BindProperty(SupportsGet = true)] public bool Saved { get; set; }` mirroring the existing `Uid` bind. No other handler change needed — the flag arrives in the query string from the 303 Location and binds on GET.

    Modify `Pages/Roles/Detail.cshtml`: after the page title / before the field content, add `@if (Model.Saved)` -> `<div class="alert alert-success alert-dismissible fade show" role="alert">Role saved.<button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button></div>` (mirror the existing `alert-danger` structure, UI-SPEC copy "Role saved.", green `--bs-success`). This is the D-08 mechanism: the `?saved=1` flag travels in the redirect Location URL set by `TurboRedirect` in 04-02 Task 1, survives the 303 natively, and is read here — NO TempData. The redirect target is a server-constructed `/roles/{uid}` path (never user-supplied), closing the open-redirect concern (RESEARCH Security).
  </action>
  <acceptance_criteria>
    - Source: `Pages/Roles/Detail.cshtml.cs` contains `public bool Saved`. Verify: `grep -c 'public bool Saved' Pages/Roles/Detail.cshtml.cs` returns 1.
    - Source: `Pages/Roles/Detail.cshtml.cs` `Saved` is a `[BindProperty(SupportsGet = true)]`. Verify: read confirms the attribute precedes `Saved`.
    - Source: `Pages/Roles/Detail.cshtml` contains the banner gated on `Model.Saved` with copy "Role saved." and `alert-success`. Verify: `grep -c 'Role saved.' Pages/Roles/Detail.cshtml` returns 1 and `grep -c 'alert-success' Pages/Roles/Detail.cshtml` returns >= 1.
    - Source: no TempData on the detail page. Verify: `grep -c 'TempData' Pages/Roles/Detail.cshtml.cs Pages/Roles/Detail.cshtml` returns 0.
    - Build: `dotnet build Backlot.Studio/Backlot.Studio.csproj` succeeds.
    - Behavior (against running API): after a successful save, the detail page at /roles/{uid}?saved=1 shows the green dismissible "Role saved." banner; navigating to /roles/{uid} without the flag shows no banner.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build Backlot.Studio/Backlot.Studio.csproj 2>&1 | grep -E 'Build succeeded|error'</automated>
    <human-check>Save a role successfully -> land on detail with green "Role saved." banner (dismissible). Visit the detail page directly (no ?saved=1) -> no banner. Confirm no TempData used.</human-check>
  </verify>
  <done>
    The detail page reads `?saved=1` and shows a dismissible green "Role saved." banner after a successful edit, with no TempData. EDIT-03 fully delivered (save + 303 redirect + success confirmation).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Studio (edit POST) | Untrusted field values + hidden Uid cross here; payload is forwarded to isvalid/persist |
| Studio -> Backlot API | Validation and persistence are owned by Backlot; Studio orchestrates with session Basic Auth |
| 303 redirect Location -> Detail GET | The `?saved=1` flag travels in a server-constructed URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering (mass assignment / field injection) | `OnPostAsync` / `BuildPayload` | mitigate | Payload is built by iterating the SCHEMA field list, never the posted keys; Calculated/read-only fields are skipped; only schema-known fields plus the hidden Uid are sent to isvalid/persist. (RESEARCH Security V5.) |
| T-04-02 | Elevation (editing without CanWrite) | `OnPostAsync` | mitigate | Re-check `__Permission.CanWrite` server-side before persisting (defense-in-depth; Backlot API also enforces `IPermission`). |
| T-04-05 | Tampering (reflected XSS on 422 re-render) | validation summary + field values | mitigate | Razor auto-HTML-encodes `@e.ErrorMessage` and `value="@val"`; no `Html.Raw` on error or field text. |
| T-04-06 | Tampering (open redirect via success target) | `TurboRedirect("/roles/{uid}?saved=1")` | mitigate | Redirect target is a server-constructed `/roles/{uid}` path built from the bound Uid, never a user-supplied URL. |
| T-04-07 | Spoofing/Tampering (CSRF on POST) | edit POST handler | mitigate | Razor Pages auto-validates antiforgery; the Turbo-driven form carries `@Html.AntiForgeryToken()` (verified in 04-01). |
| T-04-SC | Tampering (supply chain) | package installs | accept | Zero packages installed this phase (RESEARCH Package Legitimacy Audit: N/A); no CDN additions. |
</threat_model>

<verification>
- `dotnet build Backlot.Studio/Backlot.Studio.csproj` succeeds.
- OnPostAsync: isvalid-first, 422-on-invalid with summary, persist-on-valid, 303-on-success, persist-failure preserves values.
- No `TempData` anywhere in the edit or detail pages (D-08 hard constraint).
- The `?saved=1` banner renders on Detail only when the flag is present.
- Payload is schema-driven (mass-assignment-safe), errors auto-encoded (no Html.Raw).
</verification>

<success_criteria>
- EDIT-02: Before saving, validation errors from `role/isvalid` are shown to the user — as a top-of-form summary block per the recorded D-07 deviation (verification treats summary-block as intended scope, not a gap).
- EDIT-03: User saves via `persist/persist`; success -> 303 redirect to the detail page with a "Role saved." banner; validation failure -> 422 re-render with errors; persist failure -> form preserved with an error message.
- No TempData used (D-08).
</success_criteria>

<output>
Create `.planning/phases/04-role-editing/04-02-SUMMARY.md` when done. Note the confirmed end-to-end behavior (422 summary on invalid; 303 + banner on valid), and whether the defensive isvalid parse needed the generic-fallback path on any real role.
</output>
