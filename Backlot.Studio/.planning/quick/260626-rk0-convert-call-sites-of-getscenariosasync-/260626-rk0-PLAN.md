---
phase: quick-260626-rk0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Backlot.Studio/Pages/Scenarios/Index.cshtml.cs
  - Backlot.Studio/Pages/Roles/Index.cshtml.cs
  - Backlot.Studio/Pages/Roles/Relations.cshtml.cs
  - Backlot.Studio/Pages/Roles/Detail.cshtml.cs
  - Backlot.Studio/Pages/Roles/Edit.cshtml.cs
  - Backlot.Studio/Services/BacklotApiClient.cs
  - Backlot.Studio/Services/IBacklotApiClient.cs
autonomous: true
requirements: []

must_haves:
  truths:
    - "Scenarios, Roles list, Role detail, Relations, and Edit pages still load and behave identically (same HTTP requests issued)"
    - "Edit page validation (422 with per-field results) and save (303 redirect) still work unchanged"
    - "The 7 typed wrapper methods no longer exist on BacklotApiClient or IBacklotApiClient"
  artifacts:
    - path: "Backlot.Studio/Services/BacklotApiClient.cs"
      provides: "Play* primitives + IsAuthenticatedAsync/WhoAmIAsync + public UnwrapRoleDetail; 7 typed wrappers removed"
    - path: "Backlot.Studio/Services/IBacklotApiClient.cs"
      provides: "Interface with only the surviving members"
  key_links:
    - from: "Backlot.Studio/Pages/Roles/Detail.cshtml.cs"
      to: "Backlot.Studio/Services/BacklotApiClient.cs"
      via: "BacklotApiClient.UnwrapRoleDetail(env.Body) static call"
      pattern: "UnwrapRoleDetail"
---

<objective>
Behavior-preserving refactor: rewire all 7 typed call sites of the BacklotApiClient
wrapper methods to call the generic `PlayAsync` / `PlayAllowingClientErrorAsync` primitives
directly, then delete the 7 now-unused typed methods from both the implementation and the
interface. Issued HTTP requests stay byte-for-byte identical.

Purpose: Collapse redundant typed wrappers onto the generic primitives added in 260626-ou7,
shrinking the API client surface to one convention-based primitive set.
Output: 5 PageModel files rewired, 2 service files trimmed, `UnwrapRoleDetail` made public.
</objective>

<execution_context>
@/home/jeroen/Projects/Backlot/.claude/gsd-core/workflows/execute-plan.md
@/home/jeroen/Projects/Backlot/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@Backlot.Studio/Services/BacklotApiClient.cs
@Backlot.Studio/Services/IBacklotApiClient.cs
@Backlot.Studio/Pages/AuthenticatedPageModel.cs
@Backlot.Studio/Pages/Scenarios/Index.cshtml.cs
@Backlot.Studio/Pages/Roles/Index.cshtml.cs
@Backlot.Studio/Pages/Roles/Detail.cshtml.cs
@Backlot.Studio/Pages/Roles/Edit.cshtml.cs
@Backlot.Studio/Pages/Roles/Relations.cshtml.cs

Mapping (path tokens MUST stay byte-for-byte identical):
- GetScenariosAsync()        -> PlayAsync<IEnumerable<ScenarioItem>>("director", "scenarios")
- GetRoleSchemaAsync()       -> PlayAsync<IReadOnlyList<RoleSchema>>("director", "roles")
- FindRolesAsync(request)    -> PlayAsync<FindResult>("simplequery", "find", request)
- GetRoleRelationsAsync(uid) -> PlayAsync<IEnumerable<RelationItem>>("persist", "relations", new { Uid = uid })
- PersistRoleAsync(payload)  -> PlayAsync<JsonElement>("persist", "persist", payload)
- ValidateRoleAsync(payload) -> PlayAllowingClientErrorAsync<ValidationOutcome>("role", "isvalid", payload)
- GetRoleDetailAsync(uid)    -> PlayAsync<JsonElement>("seekbase", "detail", new { For = uid }) THEN unwrap

`SafeApiCall<T>` returns `(T? Value, IActionResult? Redirect)`. Wrapping `PlayAsync<X>(...)`
makes Value an `ApiEnvelope<X>?`; read `?.Body`. `ApiEnvelope<JsonElement>.Body` is a
non-nullable JsonElement value, directly passable to `UnwrapRoleDetail`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire all 7 call sites to the generic primitives</name>
  <files>Backlot.Studio/Pages/Scenarios/Index.cshtml.cs, Backlot.Studio/Pages/Roles/Index.cshtml.cs, Backlot.Studio/Pages/Roles/Relations.cshtml.cs, Backlot.Studio/Pages/Roles/Detail.cshtml.cs, Backlot.Studio/Pages/Roles/Edit.cshtml.cs</files>
  <action>
Rewire each call site to call the generic primitive directly through the existing `_api`
field, reading `?.Body` off the returned envelope. Preserve ALL surrounding control flow,
redirect-guards, error messages, comments, BuildPayload/MatchSchema logic, the WR-01
location encoding, and the validation fallback exactly as-is.

Scenarios/Index.cshtml.cs (OnGetAsync): replace the `_api.GetScenariosAsync()` call with
`_api.PlayAsync<IEnumerable<ScenarioItem>>("director", "scenarios")`. The tuple Value
(`result`) is now `ApiEnvelope<IEnumerable<ScenarioItem>>?`; change the grouping source from
`(result ?? [])` to `(result?.Body ?? [])`. Keep the `if (redirect != null) return redirect;`
line and the rest of the LINQ unchanged.

Roles/Index.cshtml.cs (OnGetAsync): replace `_api.FindRolesAsync(request)` with
`_api.PlayAsync<FindResult>("simplequery", "find", request)`. Change `RoleResult = result;`
to `RoleResult = result?.Body;` (RoleResult stays `FindResult?`).

Roles/Relations.cshtml.cs (OnGetAsync): replace `_api.GetRoleRelationsAsync(Uid)` with
`_api.PlayAsync<IEnumerable<RelationItem>>("persist", "relations", new { Uid = Uid })`.
Change `Relations = result ?? [];` to `Relations = result?.Body ?? [];`.

Roles/Detail.cshtml.cs (OnGetAsync): replace `_api.GetRoleDetailAsync(Uid)` with
`_api.PlayAsync<JsonElement>("seekbase", "detail", new { For = Uid })`. The tuple Value is now
`ApiEnvelope<JsonElement>?` (name it `env`). Keep `if (redirect != null) return redirect;`,
then set `RoleData = env is null ? null : BacklotApiClient.UnwrapRoleDetail(env.Body);`.
Leave the subsequent `RoleData.HasValue` block unchanged.

Roles/Edit.cshtml.cs — FOUR call sites, all via SafeApiCall, keeping every redirect-guard
(`if (rN != null) return rN;`) intact:
  - OnGetAsync: GetRoleDetailAsync -> PlayAsync<JsonElement>("seekbase","detail", new { For = Uid });
    set `detail` (JsonElement?) via `detail = env is null ? null : BacklotApiClient.UnwrapRoleDetail(env.Body);`
    where `env` is the tuple Value. GetRoleSchemaAsync -> PlayAsync<IReadOnlyList<RoleSchema>>("director","roles");
    set `schema` to the envelope Value's `?.Body`. Downstream `detail.HasValue` / `detail.Value`
    and `schema != null` usage stays unchanged.
  - OnPostAsync: GetRoleSchemaAsync -> PlayAsync<IReadOnlyList<RoleSchema>>("director","roles")
    with `schema = env?.Body`; GetRoleDetailAsync -> PlayAsync<JsonElement>("seekbase","detail", new { For = Uid })
    with `detail = env is null ? null : BacklotApiClient.UnwrapRoleDetail(env.Body)`;
    ValidateRoleAsync -> PlayAllowingClientErrorAsync<ValidationOutcome>("role","isvalid", payload):
    capture the envelope then assign `var outcome = outcomeEnv?.Body;` so the existing
    `outcome is null || !outcome.IsValid` and `outcome?.Results` block (and its comments and the
    `results.Count > 0 ? results : [new ValidationResultItem{...}]` fallback) stay unchanged;
    PersistRoleAsync -> PlayAsync<JsonElement>("persist","persist", payload) (result discarded:
    `var (_, r3) = ...`). Keep the WR-01 `Url.Page(...)` location encoding and `TurboRedirect` unchanged.

Each detail site uses the static `BacklotApiClient.UnwrapRoleDetail(...)`; Detail.cshtml.cs and
Edit.cshtml.cs already import `using Backlot.Studio.Services;`, so no new using is required (add
it only if a build error reports the namespace is missing).

Do NOT delete any methods in this task — deletion happens in Task 2 so the build stays green
between tasks is not required, but the call-site edits here must compile against the new
signatures once Task 2 makes UnwrapRoleDetail public.
  </action>
  <verify>
    <automated>grep -rn "result?.Body\|env?.Body\|UnwrapRoleDetail(env.Body)\|outcomeEnv?.Body" Backlot.Studio/Pages/ | head</automated>
  </verify>
  <done>All 5 PageModel files call PlayAsync/PlayAllowingClientErrorAsync directly and read .Body; no remaining references to the 7 typed wrapper methods in Pages/.</done>
</task>

<task type="auto">
  <name>Task 2: Make UnwrapRoleDetail public, delete 7 wrappers from impl + interface, build</name>
  <files>Backlot.Studio/Services/BacklotApiClient.cs, Backlot.Studio/Services/IBacklotApiClient.cs</files>
  <action>
In BacklotApiClient.cs: change the signature of `private static JsonElement UnwrapRoleDetail(JsonElement body)`
to `public static JsonElement UnwrapRoleDetail(JsonElement body)`. Keep its body and comments
intact; do NOT move it.

Delete these 7 methods (and their leading doc comments) from BacklotApiClient.cs:
GetScenariosAsync, FindRolesAsync, GetRoleDetailAsync, GetRoleRelationsAsync, GetRoleSchemaAsync,
ValidateRoleAsync, PersistRoleAsync.

Delete the matching 7 interface members from IBacklotApiClient.cs (lines declaring
GetScenariosAsync, FindRolesAsync, GetRoleDetailAsync, GetRoleRelationsAsync, GetRoleSchemaAsync,
ValidateRoleAsync, PersistRoleAsync).

KEEP in both files: IsAuthenticatedAsync, WhoAmIAsync, the three Play* primitives,
EnsureSuccessAsync, JsonOptions, BacklotApiException, and the now-public UnwrapRoleDetail.
Keep the interface's PlayAsync/PlayAllowingClientErrorAsync doc comment block.
  </action>
  <verify>
    <automated>dotnet build Backlot.Studio/Backlot.Studio.csproj 2>&1 | tail -5; for m in GetScenariosAsync FindRolesAsync GetRoleDetailAsync GetRoleRelationsAsync GetRoleSchemaAsync ValidateRoleAsync PersistRoleAsync; do echo "$m: $(grep -rn "$m" Backlot.Studio --include=*.cs | wc -l)"; done</automated>
  </verify>
  <done>dotnet build succeeds with 0 errors; each of the 7 deleted method names returns 0 references across Backlot.Studio *.cs files; UnwrapRoleDetail is public.</done>
</task>

</tasks>

<verification>
- `dotnet build Backlot.Studio/Backlot.Studio.csproj` completes with 0 errors.
- `grep -rn` for each of the 7 deleted method names across Backlot.Studio returns 0 matches.
- IsAuthenticatedAsync, WhoAmIAsync, and the three Play* primitives remain on both impl and interface.
</verification>

<success_criteria>
All call sites use the generic primitives; the 7 typed wrappers are deleted from both
BacklotApiClient.cs and IBacklotApiClient.cs; UnwrapRoleDetail is public and called from the
3 detail sites; the build is clean and no HTTP path tokens changed.
</success_criteria>

<output>
Create `.planning/quick/260626-rk0-convert-call-sites-of-getscenariosasync-/260626-rk0-SUMMARY.md` when done
</output>
