
<nav class="flex flex-col h-full bg-white border-r border-secondary-200">
    <!-- Logo/Brand section -->
    <div class="p-4 border-b border-secondary-200">
        <div class="flex items-center gap-3">
            <div class="w-10 h-10 bg-gradient-to-br from-primary-500 to-primary-700 rounded-lg flex items-center justify-center">
                <i class="bi bi-palette text-white text-xl"></i>
            </div>
            <div class="sidebar-label">
                <h1 class="font-bold text-secondary-900">Backlot Studio</h1>
                <span class="text-xs text-secondary-500">Administrator</span>
            </div>
        </div>
    </div>

    <!-- Navigation sections -->
    <div class="flex-1 p-4">
        <!-- Main navigation -->
        <div class="mb-6">
            <h3 class="text-xs font-semibold text-secondary-400 uppercase tracking-wider mb-2 px-3 sidebar-label">Main</h3>
            <ul class="space-y-1">
                <li>
                    <a href="/"
                       class="flex items-center gap-3 px-3 py-2 rounded-lg sidebar-label text-secondary-600 hover:bg-secondary-100 hover:text-secondary-900"
                       data-turbo-action="advance">
                        <i class="bi bi-speedometer2 text-lg text-secondary-400"></i>
                        <span>Dashboard</span>
                    </a>
                </li>
                <li>
                    <a href="/scenarios"
                       class="flex items-center gap-3 px-3 py-2 rounded-lg sidebar-label text-secondary-600 hover:bg-secondary-100 hover:text-secondary-900"
                       data-turbo-action="advance">
                        <i class="bi bi-play-circle text-lg text-secondary-400"></i>
                        <span>Scenarios</span>
                    </a>
                </li>
                <li>
                    <a href="/roles"
                       class="flex items-center gap-3 px-3 py-2 rounded-lg sidebar-label text-secondary-600 hover:bg-secondary-100 hover:text-secondary-900"
                       data-turbo-action="advance">
                        <i class="bi bi-people text-lg text-secondary-400"></i>
                        <span>Roles</span>
                    </a>
                </li>
            </ul>
        </div>

        <!-- Additional sections -->
        <div class="mb-6">
            <h3 class="text-xs font-semibold text-secondary-400 uppercase tracking-wider mb-2 px-3 sidebar-label">Tools</h3>
            <ul class="space-y-1">
                <li>
                    <a href="#" 
                       class="flex items-center gap-3 px-3 py-2 rounded-lg sidebar-label text-secondary-600 hover:bg-secondary-100 hover:text-secondary-900"
                       data-turbo-action="advance">
                        <i class="bi bi-gear text-lg text-secondary-400"></i>
                        <span>Settings</span>
                    </a>
                </li>
            </ul>
        </div>
    </div>

    <!-- User profile section -->
    <div class="p-4 border-t border-secondary-200">
        <div class="flex items-center gap-3">
            <div class="w-10 h-10 bg-gradient-to-br from-primary-100 to-primary-200 rounded-full flex items-center justify-center">
                <span class="text-primary-700 font-medium">@(ViewData["Username"]?.ToString()?.Substring(0, 1)?.ToUpper() ?? "U")</span>
            </div>
            <div class="flex-1 min-w-0 sidebar-label">
                <p class="font-medium text-secondary-900 truncate">@ViewData["Username"]</p>
                <p class="text-xs text-secondary-500">Administrator</p>
            </div>
        </div>
    </div>
    
</nav>
